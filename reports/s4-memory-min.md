# S4 · 记忆最小实现：验收报告

> 日期：2026-09-22 · 阶段：**S4** · 结论标注：**已核查 / 待实测 / 推测**
> 代码：`src/nova/memory.py`（核心）、`src/nova/layers.py`（`capture_slice`）、`src/nova/decode.py`（分段 prefill）、`src/chatfmt.py`（`find_span`）
> 演示：`src/s4_memory_demo.py` · 基准：`src/bench_memory.py` · 诊断：`src/diagnostics/probe_memory*.py`（6 个）
> 测试：`tests/test_memory.py`（**12 passed**，全套 **40 passed**）
> 关联决策：**D31**（本阶段落地）· 硬约束 **D06 / D07 / D09 / D17**

---

## 一、验收结论

| 判据（[HANDOFF.md](../HANDOFF.md) 第二节） | 要求 | 实测 | 结论 |
|------|------|------|:---:|
| 第 1 轮写入 → 第 20 轮取回 | 答对 | **8/8 全对**（6 条事实 × 8 个问题，含 3 个改写问法）—— ⚠️ 但这条**不能**解读为"原版模型会忘"，见**第八节** | ✅ 已核查 |
| 存盘重启后仍可取回 | 跨进程 | **写一个进程、问另一个进程** → 8/8 | ✅ 已核查 |
| 记忆在模型内部（D06） | 不转文本、不查外部库 | 记忆 = 模型自己的 K/V 张量，全程不出模型 | ✅ 已核查 |
| safetensors 持久化（D07） | 张量落盘 | `demo.safetensors` 16.41 MiB，读写逐位一致 | ✅ 已核查 |
| 表示空间冻结（D09） | 不匹配拒绝加载 | schema / 模型指纹不匹配 → `MemorySchemaMismatch` | ✅ 已核查 |
| 显存 < 7GB（D17） | 双通路 + 记忆 + 1024 槽位 cache | 峰值 **5.17 GiB** | ✅ 已核查 |

**`pytest tests -q` → 40 passed**（其中 `tests/test_memory.py` 12 passed）。

---

## 二、落地的机制

### 2.1 为什么不是 docs/03 里那个 `[128, 2560]` 记忆令牌

[docs/03-memory-system.md](../docs/03-memory-system.md) 设计的记忆令牌（`[128, 2560]`）配 `[128, 512]` 独立键，是**要训练的**写入器 / 读取器 —— 模型得先学会"什么值得记""怎么生成记忆令牌"，属于**里程碑 2**。

S4 的验收标准不训练也要能达成，所以先落地 [docs/09-speculative.md](../docs/09-speculative.md) 的记忆精度分级里的 **L0 精确 KV 层**：

| 环节 | S4 做法 |
|------|------|
| **写入** | 抓记忆区间在**每个 cache 槽位**上的 K（RoPE 之前）/ V |
| **寻址** | 用模型**自己的** Q / K 做键值联想（`Q·K` 注意力质量），无额外参数、无需训练 |
| **读取** | 取回的 K/V 按新位置重新旋转，写进静态 KV cache 的指定槽位，模型照常生成 |

键用"模型自己的 Q/K"，等价于 D09 的"记忆模块与语言模型解耦"在 L0 的自然形式：**不新增任何投影层**，所以不存在"记忆模块被训练带动而漂移"的问题。

### 2.2 写入：抓在哪一层

`capture_qkv` 在**不带 cache 的一次完整前向**上抓取（K/V 依赖因果上下文，输入必须是"整段对话"，抓到的才是这段记忆在真实上下文里的状态）。它挂在 `LeanAttention` 上的新钩子 `capture_slice`：命中区间时把 **RoPE 之前**的 Q/K/V 存进 `captured`。图解码路径的 `capture_slice` 必须保持 `None`，否则会污染 CUDA Graph 捕获。

### 2.3 检索：为什么必须按"注入后的真实位置"旋转

这是 S4 花了 7 轮诊断才定下来的地方，结论如下（详见第四节）：

1. **不能用 pre-RoPE 裸余弦** —— 记忆的键有一个很强的**公共分量**，裸余弦对 6 条候选**全部 > 0.8**，几乎不区分内容。
2. **必须按注入后的真实位置旋转** Q/K 再算 `Q·K` —— 否则查询（位置 600）和记忆（位置 0）不在同一坐标系，打分退化成噪声（top-1 只有 1/3）。
3. **用 softmax 质量而不是裸分数** —— softmax 会减掉那个公共加性偏移。

### 2.4 注入位置：记忆插在"当前轮之前"

`place="turn"`：记忆插在 `history` 与 `current` 之间，与问题的距离只有当前轮的长度。

`place="front"`（文档原设计的"拼在最前面"）在 20 轮历史下**寻址退化到 1/3，且生成质量崩**（开始复读"今天多云转晴"）—— RoPE 的长距离把 `Q·K` 抹平了。这条写进 D31。

---

## 三、机制证据：注入与真实 prefill 逐位一致（已核查）

把整段 prompt 抓成记忆再按位置注入，得到的 KV cache 与"真实 prefill"**逐位相同**：

| 项 | 结果 |
|------|:---:|
| 比对范围 | 60 个 cache 槽位 × K 与 V |
| 逐位最大差 | **0.000e+00** |

这一条同时锁死了三件事：捕获抓的确实是 RoPE 之前的值（否则旋转两次就错了）、槽位映射没错、注入时按新位置重新旋转是对的。已固化成 `test_injection_reproduces_prefill`。

**通路 0 / 1 的捕获逐位相同**（门控关闭时，已核查）—— 意味着文件里存两份是 2× 冗余，见第六节"待办"。

---

## 四、寻址设计：扫描与选型（已核查）

6 条候选记忆 × 8 个问题（含 3 个改写问法，不给词面线索），12 组配置全扫（脚本 `src/diagnostics/probe_memory_addressing*.py`，结果 `.tmp/s4-retrieval-sweep.json`）：

| 坐标系 | 标准化 | 打分 | top-1 | top-2 |
|------|:---:|------|:---:|:---:|
| `alone` | 否 | **按长度归一** | **8/8** | 8/8 |
| `alone` | 否 | 不归一 | 7/8 | 8/8 |
| `alone` | 否 | max-logit | 7/8 | 8/8 |
| `alone` | 是 | 按长度归一 | 7/8 | 7/8 |
| `alone` | 是 | 不归一 | 4/8 | 8/8 |
| `stack` | 是/否 | 任意 | 2/8 | 6–8/8 |

三个默认值都是这样选出来的：

- **`frame="alone"`**：每条记忆单独注入（与 `top_k=1` 的真实情况一致）。`stack`（全部堆在一起）只有 2/8 —— 因为真实推理时只注入 top-1，打分时的坐标系必须和真实情况一致。
- **按 token 数归一**：不归一的话长记忆天然占便宜（质量按 token 累加）。
- **不标准化**：`standardize=True` 在 3 条候选时看着有用（2/3），候选变多后反而变差（4/8）。

**查询取"问题那句话的末尾 4 个 token"**。两个踩过的坑：

- 取 prompt 最后 4 个 token → 拿到的是 `<|im_start|>assistant\n`，**没有内容**，寻址退化成"恒选第一条"。
- 整句取平均 → 8/8 掉到 7/8。

这条**测试脚本与实现必须走同一条路径**。第一版 `tests/test_memory.py` 自己在测试里又手搓了一遍打分，忘了把 span 截成末尾 4 个 token，命中率立刻从 8/8 掉到 7/8 —— 现在检索逻辑只写一份（`MemorySession.rank()`），`prefill` 与测试都调它。

---

## 五、端到端：写入 → 20 轮无关对话 → 提问（已核查）

**场景**：第 1 轮用户说了 6 件事（红裙子 / 橘猫 / 花瓶 / 咖啡 / 出差 / 吉他），抓成 6 条记忆存盘，**第 1 轮从可见历史里去掉**（= 压缩进记忆，不再占 KV cache），插 20 轮无关闲聊（608 token），第 22 轮提问。每个问题跑三种条件。

| 条件 | 说明 | 8 个问题答对 |
|------|------|:---:|
| `auto` | 自动检索 top-1 并注入 | **8/8** |
| `off` | 不注入（对照） | **0/8** |
| `swap` | 故意注入**另一条**记忆 | **0/8** |

- `off` 全错的表现是**反问或编造**（"我无法看到你穿的裙子，因为我是AI，没有视觉功能"、"你家的猫名字还没告诉我呢 😊 是'小橘'？"）—— 证明模型自己不知道。
- `swap` 全错：注入了另一条，答案就跟着变，**原答案不再出现**。这是"答案来自注入内容"的因果证据。

代表输出（`auto`）：

```
[auto] 你今天穿的是红色的裙子哦！   （这是你上周在巴黎买的那条～）
[auto] 你养的猫叫雷纳德，是一只橘猫。
[auto] 你把阳台上的蓝色花瓶打碎了。
[auto] 你下周要去**成都**出差三天。
```

### 存盘重启（跨进程）证据

`--phase write` 与 `--phase ask` 是**两个独立进程**（各自重新加载模型与 tokenizer），中间只通过 `.safetensors` 文件传递：

```
[写入] 第 1 轮 prompt 75 token（6 条事实）
       [0] 红裙子   token (3, 17) -> 14 个位置
       ...
[写入] MemoryStore(items=6, tokens=70, 16.41 MiB, schema=cac396b8)
[写入] 存盘 -> .tmp\s4-memory\demo.safetensors（16.41 MiB）
[写入] 表示空间 schema=cac396b86277 模型指纹=3354283f9349

[读取] 载入 MemoryStore(items=6, tokens=70, 16.41 MiB, schema=cac396b8)  （外来=False）
[读取] 可见历史 608 token = 20 轮无关对话（第 1 轮已被记忆替代）
[汇总] auto 条件下答案正确 8/8
```

日志：`.tmp/s4-write.log`、`.tmp/s4-ask.log`。

---

## 六、开销与显存（已核查；速度数字带 clocks.sm）

**记忆体积**：6 条 = 70 个位置 = **16.41 MiB**，即 **240 KiB / token**（60 槽位 × 8 KV 头 × 128 维 × K,V × fp16 × 2 通路）。这是**无损、不压缩**的代价，随区间线性增长。

**每轮额外开销**（608 token 历史 + 17 token 当前轮，双通路 triton，`src/bench_memory.py`）：

| 阶段 | clocks.sm 2.4–2.5 GHz（94 W） | clocks.sm 0.87–1.28 GHz（~37 W） |
|------|:---:|:---:|
| prefill 基线（不注入） | 559.6 ms | 1252.2 / 1255.5 ms |
| 检索（取 Q + 打分） | 549.4 ms（取 Q 430 + 打分 40） | 1188.7 / 1185.3 ms（取 Q 943 + 打分 90） |
| 检索 + 注入 + prefill | 1220.3 ms | 2395.3 / 2382.0 ms |

**逐轮交替测（同一时钟区间内比 on/off，取差值中位；3 / 7 轮两次独立测量）：**

| 项 | 值 |
|------|:---:|
| 记忆的额外开销 | **1178 / 1190 ms** |
| ├ 取 Q | 969 / 953 ms |
| ├ 打分 | 90 / 90 ms |
| └ 注入 | **−41 / −12 ms** |

**与时钟无关的比值**（跨时间点可比，这是最该记的数字）：

- **检索 / 基线前向 = 0.94–0.98x** —— 取 Q 要跑一次**完整无 cache 前向**（K/V 依赖因果上下文，必须整段跑），成本≈再来一次 prefill。
- **检索+注入 / 基线前向 = 1.90–2.18x** —— 记忆让每轮贵了约一倍。

**注入本身几乎不花时间**（三次独立测量都是负值）。**推测**原因：`place="turn"` 的分段 prefill 把注意力算成 `608² + 17×639`，而单段是 `625²`，二次注意力反而省 ~2.6%（≈31 ms @1.2 s），与实测 −40 ms 同量级；注入只是 720 次 KV 切片写入（按 D27 的 ~13.5 µs/launch ≈ 10 ms）。

**显存**：峰值 **5.17 GiB**（双通路 + 记忆 + max_len=1024），D17 的 7GB 预算内。

⚠️ **本机时钟陷阱（已核查）**：连测 7 轮会让笔记本 GPU 从 2160 MHz 掉到 ~870 MHz、功耗 94 W → 35 W，**同一条件的耗时直接翻倍**。所以上表第二列与第一列不能互相换算，报告里两个时钟区间的数字各自成组。基准已内置预热与逐轮时钟采样（`--warmup`，打印每档 `clocks.sm` 区间）。

---

## 七、已知边界（不要当成已解决）

| 项 | 状态 |
|------|------|
| **无损，不是压缩** | 记忆 = 该区间全部 token 的 K/V，体积随区间线性增长。L1 高保真嵌入 / L2 压缩摘要留给里程碑 3 |
| **键未训练** | 区分度是**实测**出来的，不是设计保证的。改写问法（"我家的宠物是什么品种？"→ 橘猫）能命中，但没有理论保证 |
| **LoRA / 微调会移动 K/V 空间** | D09 的"表示漂移"风险 —— 记忆匹配质量在训练后的衰减**待实测**（里程碑 2 的必测项） |
| **通路 0/1 各存一份** | 门控关闭时两条通路逐位相同（已核查），去重可省一半空间。留到交叉注意力真正打开后再定形 |
| **检索不能跨轮复用** | 每轮历史都在增长，取 Q 必须重跑 |
| **`place="front"` 退化** | 20 轮历史下寻址 1/3 且生成崩。只能用 `turn` |
| **20 轮这个尺度上记忆没有收益** | 原版模型 120 轮（3785 token）内全对，上下文上限 262144 —— 记忆的价值在**跨会话持久**与**超显存长度**，不在"省轮数"（第八节） |

**下一步优化（按收益排序，尚未实施）**：

1. **检索从 ~1x prefill 降到 ~0**（最大收益，**推测**可行）：取 Q 只需要问题那几个 token 的 Q，而 **pre-RoPE 的 Q 与位置无关** —— 生成时反正要 prefill 历史，可以复用那份 cache 的隐藏状态，只为当前轮 17 个 token 单独取 Q。这样每轮省下近一次完整前向（≈950 ms @低时钟 / ≈430 ms @高时钟）。
2. **`--dedup` 只存一条通路**，省一半记忆体积。
3. **把 `scores` 的逐条旋转向量化**（现在是 6 次 Python 循环）。

---

## 八、对照实验：原版模型到底会不会忘（已核查）

**这一节是对本报告早期说法的更正，必须先看。**

S4 演示里的 `off = 0/8` 有两种可能解释，不分开就会得出错误结论：

- **(a)** 模型**看到过**第 1 轮，但 20 轮后**忘了** → 那 S4 是在解决真问题；
- **(b)** 第 1 轮被从可见历史里**删掉**了 → 模型**从未见过**，`off=0/8` 只是"记忆确实起作用"的**因果对照**。

**S4 演示测的是 (b)。** 所以"20 轮就忘了"这个说法**不成立**，必须实测 (a)。

`src/diagnostics/exp_recall_baseline.py`：把第 1 轮**留在**可见历史里，后面追加 N 轮无关对话，**不注入任何记忆**：

| 可见历史 | token | 答对 |
|------|:---:|:---:|
| 第 1 轮 + 20 轮 | 683 | **8/8** |
| 第 1 轮 + 60 轮 | 1915 | **8/8** |
| 第 1 轮 + 120 轮 | 3785 | **8/8** |

**结论（已核查）：原版 Qwen3-VL-4B 在 120 轮（3785 token）内一字不差地记得第 1 轮。** 该模型上下文上限 **262144 token** —— 闲聊要撑爆它需要约 7000 轮，20 轮完全不是瓶颈。

### 那 S4 到底证明了什么、没证明什么

| | 状态 |
|------|------|
| **证明了**：记忆的读写通路能工作 —— 从模型内部状态抓取 → 存成 `.safetensors` → 另一进程读回 → 按位置注入 → 模型照常使用；且**逐位无损**，寻址 8/8，因果干净（off / swap 都是 0/8） | ✅ 已核查 |
| **没证明**：记忆比"直接把历史留在上下文里"更好 | ⚠️ **20 轮这个尺度上，记忆没有任何收益** |

**这不是白做，但价值点要重新定位。** S4 真正的不可替代之处不在于"省 token"，而在于：

1. **跨会话持久** —— 上下文窗口在关掉程序的那一刻就清零了。`write` / `ask` 两个进程的演示恰好是这一点：新进程里模型对那 6 件事**一无所知**，只有存档能把它带回来。**这才是 `off=0/8` 真正对应的场景。**
2. **显存是硬墙，比窗口更早撞上** —— 本机 8GB（D17 预算 < 7GB，模型已占 4.83 GiB）留给 KV cache 的只有约 2.2 GB。按实测 **240 KiB/token**（双通路）算，**约 9000 token 就到顶**（单通路 144 KiB/token，约 15000 token）。这才是"上下文装不下"的真实门槛，而不是 262144 的模型窗口。
3. **里程碑 2/3 的巩固 / 遗忘 / 多时间尺度记忆需要这条通路** —— 那些机制全都要"把一段状态写下来、以后再取回来"，S4 是它们的地基。

**待实测（下一步该做的对照）：** 在**超过显存能装下的长度**上比"记忆 + 短上下文"与"全部塞进上下文"，看记忆是否真的换来可用性。那才是记忆的收益证明；20 轮这个尺度证明不了。

---

## 九、复现命令

```powershell
$env:HF_HOME=(Resolve-Path .).Path+'\.hf-cache'; $env:TMP=(Resolve-Path .).Path+'\.tmp'; $env:TEMP=$env:TMP
$env:HF_HUB_OFFLINE='1'; $env:TRITON_CACHE_DIR=$env:TMP+'\triton-cache'; $env:TORCHINDUCTOR_CACHE_DIR=$env:TMP+'\inductor-cache'

& .\.venv\Scripts\python.exe -m pytest tests -q          # 40 passed
& .\.venv\Scripts\python.exe src\s4_memory_demo.py --phase write --mem .tmp\s4-memory\demo.safetensors
& .\.venv\Scripts\python.exe src\s4_memory_demo.py --phase ask   --mem .tmp\s4-memory\demo.safetensors
& .\.venv\Scripts\python.exe src\bench_memory.py --reps 7 --warmup 2
```
