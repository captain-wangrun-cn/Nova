# 16 · 模型是怎么被实现的：文件 · 代码 · 引擎

> 回答三个常见困惑：① 架构是靠代码实现的吗？② 怎么"汇聚成一个模型文件"？③ 需要自己写一个 llama.cpp 吗？

---

## 一、一句话答案

**模型 = 数据 + 代码，两部分缺一不可。**

- **数据（模型文件）**：权重张量 + 超参配置 + 分词器。**里面没有一行架构代码。**
- **代码（运行时）**：实现"这些张量怎么算"的计算图。它住在**库里**（transformers / llama.cpp / 你自己的引擎），**不在模型文件里**。

所以"汇聚成一个模型文件"要修正一下：**文件里汇聚的是权重和元数据，不是代码。** 架构永远是"跑它的那个程序"提供的。

---

## 二、HF 上一个模型目录的真实样子

以 Qwen3-VL-4B-Instruct 为例：

| 文件 | 内容 | 属于 |
|------|------|:---:|
| `config.json` | 超参 + **架构名字符串**（`"architectures": ["Qwen3VLForConditionalGeneration"]`、`model_type: qwen3_vl`） | 数据 |
| `model-0000x-of-0000y.safetensors` | 权重（分片） | 数据 |
| `tokenizer.json` / `vocab` / `merges` | 文本 ↔ token 映射表 | 数据 |
| `tokenizer_config.json` | **chat template**（`<|im_start|>` 那套） | 数据 |
| `preprocessor_config.json` | 图像预处理参数（VL 特有） | 数据 |
| `generation_config.json` | 默认采样参数 | 数据 |

**关键洞察：`config.json` 里的架构名只是一个"类名指针"。** 加载时 transformers 按这个字符串去库里找 `modeling_qwen3_vl.py`，实例化对应 Python 类，再把 safetensors 里的张量按名字塞进去。**文件本身是"哑"的。**

### 权重的组织方式

safetensors 就是一个**扁平字典**：`{张量名: 张量}`，例如 `model.layers.17.self_attn.q_proj.weight`。

**张量名必须与代码里的模块名逐字对应。** 这就是为什么改架构不是"改个配置文件"的事：

| 情况 | 做法 |
|------|------|
| 全新架构 | 只能从零训练 |
| 改架构但想复用权重 | 必须写**权重映射脚本**，把旧名字的张量搬到新结构的正确位置 |

> **里程碑 1 里"从 Qwen3-VL-4B 复制权重初始化两条通路"的真实含义：写映射代码，不是复制文件。**

---

## 三、GGUF 是怎么"汇聚成单文件"的

GGUF 把四样东西打成一个包：

1. 权重（已量化）
2. 元数据键值（超参、RoPE 参数）
3. 分词器词表
4. chat template

再用一个字段声明架构：`general.architecture = "qwen3vl"`。

llama.cpp 里有一堆 `build_qwen3vl()` 这样的函数，**每个架构一个**；它读到这个名字就去调对应函数搭计算图。

> **所以 GGUF 里也没有代码**——它只是把"数据 + 元数据"打包得更便携；代码在 llama.cpp 里，而且是**硬编码**的：不认识的架构名直接报错。

---

## 四、需要自己写 llama.cpp 吗？

这不是"要不要写"的二元问题，而是一条阶梯：

| 路线 | 工作量 | 速度 | 适合 |
|:---:|:---:|:---:|------|
| **A. PyTorch（自定义 nn.Module）** | 最小 | 中（4B 在 4060 上十几到几十 tok/s，需实测） | **本项目第一版** |
| B. PyTorch + torch.compile + 量化 + 手写 KV Cache | 小-中 | 中-快 | 里程碑 2 的优化 |
| C. 导出 ONNX / TensorRT | 中 | 快 | 自定义分支多时会很痛 |
| D. 自研 C++/CUDA 推理引擎 | 很大（千行级，还要自己写量化 + KV Cache + 采样） | 最快最省显存 | 长期 |
| E. 改 vLLM / SGLang 支持新架构 | 中-大 | 快 | 多用户批处理场景；单用户收益低 |

**判定规则只有一条：现成引擎只认得它内置的计算图。**

| Nova 的组件 | 现成引擎认识吗 |
|------|:---:|
| Qwen3-VL 原样单通路 | ✅ llama.cpp / vLLM / LM Studio 都行 |
| 双通路 + 交叉注意力 | ❌ |
| 预测编码门控 | ❌ |
| 工具头（不走 tokenizer 的结构化输出） | ❌ |
| 记忆令牌读写 + safetensors 持久化 | ❌（属模型侧代码，通用引擎无处安放） |

**结论：Nova 第一版只能在 PyTorch 上跑。** 这不影响开发——**训练本来就必须是 PyTorch**（自定义层 = 自定义 `nn.Module`）。速度不够时再考虑 B / C / D。

---

## 五、训练与推理是两套代码，但必须是同一个计算图

```
训练路径：PyTorch 自定义 nn.Module  ->  产出权重（safetensors）
              |
              | 必须实现同一个计算图，否则数值 / 行为对不上
              v
推理路径：PyTorch（A）或自研引擎（D）->  读出权重，重建同样的算式
```

**两条路径不一致 = 输出异常。** 这与"上次训练乱码"属于同一类事故（格式 / 实现不匹配），只是层级不同：

| 层级 | 不匹配的后果 |
|------|------|
| 数据 ↔ 分词器（chat template） | 输出乱码（**已经发生过**） |
| 训练代码 ↔ 推理代码（计算图） | 输出退化、行为诡异 |
| 权重名 ↔ 模块名（张量映射） | 直接加载失败，或**错位加载**（更危险，因为不报错） |

**实践要求：** 从 S0 起就把"模板包装""权重映射"写成可复用的单一实现（`src/chatfmt.py` 等），不许在训练 / 推理 / 评测里各写一份。

---

## 六、几个常见误解

| 误解 | 事实 |
|------|------|
| "模型文件里包含模型代码" | 不含。只有权重 + 配置 + 分词器 |
| "改架构就改改 config.json" | config.json 只能改**超参**（层数、维度）；改**结构**必须写代码 |
| "有 GGUF 就能跑任何模型" | GGUF 只是容器；llama.cpp 只认它内置的架构 |
| "部署就是把文件放上去" | 部署 = 让某个引擎实现你的计算图；自定义架构意味着自建或改造引擎 |
| "写自己的引擎才算真项目" | 反过来：第一版用 PyTorch 才是正确选择，过早写引擎是本末倒置 |

---

## 七、关联

- 部署格式抉择：[06-deployment.md](06-deployment.md) 第六节
- 里程碑 1 里"复制权重"真正要做的事：[11-roadmap.md](11-roadmap.md)
- 执行顺序与验收：[HANDOFF.md](../HANDOFF.md) 第六节（S3）
